# AGENTS.md

## Cursor Cloud specific instructions

This repository (`workClaw`) is currently an empty/newly initialized project. As of the initial setup, it contains only a `README.md` file with no application code, dependencies, or services.

### Current State

- No application code, build system, or dependency manifests exist.
- No lint, test, or build commands are available.
- No services need to be started.

### Future Development Notes

- When application code is added, update this section with:
  - How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`).
  - How to run lint, tests, and build.
  - How to start development servers or services.
  - Any non-obvious caveats discovered during setup.
